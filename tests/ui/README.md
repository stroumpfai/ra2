# Layer 3 — frontend

NiceGUI's `User` fixture, in-process and headless. Fast enough to gate commits.

Covers the shell (7 nav items in 4 groups, active item derived from the route,
title and description copy per view), the Import view's derived counts and
per-table sort state, the Census view's chips and default sort, and the
dev-sized / canary-zero markers.

Owner: **A5**.
