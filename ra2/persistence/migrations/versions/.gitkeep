# A3 writes the single initial migration covering the whole M0 schema.
# One migration author for all of phase 1 (plan-m0-m5.md §4, X3): no parallel
# heads, no merge-of-migrations. Nobody else runs `alembic revision`.
